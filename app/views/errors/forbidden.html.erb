<% content_for :title, "Not allowed" %>
<div class="py-16 text-center">
  <h1 class="text-2xl font-medium tracking-tight text-gray-900">You don’t have access to that</h1>
  <p class="mt-3 text-gray-600">Ask a church admin if you think you should.</p>
  <%= link_to "Back to dashboard", main_app.root_path, class: "mt-8 #{button_classes(:outline)}" if Current.user %>
</div>
