<%= turbo_frame_tag dom_id(@field, :frame) do %>
  <%= render "form_fields/editor", form: @form, field: @field, extra_conditions: @field.rule.conditions.empty? ? 1 : @extra_conditions.to_i %>
<% end %>
