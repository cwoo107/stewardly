Hi <%= @registration.person.first_name %>,

You're on the waitlist for <%= @registration.event.title %>, <%= l(@occurrence.starts_at, format: :long) %>.

View or cancel: <%= manage_registration_url(@registration.manage_token) %>
